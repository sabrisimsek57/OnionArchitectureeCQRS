@using Application.Dtos.TestimonialDto;

@model List<ResultTestimonialDto>

<div class="container-xxl py-6">
    <div class="container">
        <div class="text-center mx-auto mb-5 wow fadeInUp" data-wow-delay="0.1s" style="max-width: 500px;">
            <h6 class="text-primary text-uppercase mb-2">Referanslar</h6>
            <h1 class="display-6 mb-4">Müşterilerimizin Yorumları!</h1>
        </div>
        <div class="row justify-content-center">
            <div class="col-lg-8 wow fadeInUp" data-wow-delay="0.1s">
                <div class="owl-carousel testimonial-carousel">
                     @foreach(var item in Model)
                        {
                    <div class="testimonial-item text-center">
                       
                            
                     
                        <div class="position-relative mb-5">
                            
                            <img class="img-fluid rounded-circle mx-auto" src="@item.TestimonialImage" alt="">
                            <div class="position-absolute top-100 start-50 translate-middle d-flex align-items-center justify-content-center bg-white rounded-circle" style="width: 60px; height: 60px;">
                                <i class="fa fa-quote-left fa-2x text-primary"></i>
                            </div>
                        </div>
                        <p class="fs-4">@item.TestimonialDescription</p>
                        <hr class="w-25 mx-auto">
                        <h5>@item.TestimonialNamesurname</h5>
                        <span>@item.TestimonialProfession</span>
                       
                    </div>
                    }
                     
                </div>
            </div>
        </div>
    </div>
</div>
